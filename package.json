{
  "name": "example-basic",
  "type": "module",
  "scripts": {
    "dev": "deno run --allow-env --allow-sys --allow-ffi --env-file --allow-net --allow-read --allow-write --allow-run npm:vinxi dev",
    "build": "deno run --allow-env --allow-read --allow-sys --allow-run --allow-ffi --allow-write npm:vinxi build",
    "start": "deno run --allow-net --allow-read --allow-write --allow-env --allow-sys ./.output/server/index.mjs",
    "version": "vinxi version",
    "check": "deno fmt --check ./src/ && deno lint ./src/ && deno check ./src/",
    "remove-fingerprint": "deno run --allow-read --allow-write scripts/remove-fingerprint.ts"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.1.5",
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.0",
    "@solidjs/start": "^1.1.0",
    "@tailwindcss/vite": "^4.1.18",
    "@tanstack/solid-form": "^1.27.3",
    "better-auth": "^1.4.6",
    "hono": "^4.11.0",
    "hono-pino": "^0.10.3",
    "hono-rate-limiter": "^0.4.2",
    "lucide-solid": "^0.561.0",
    "mongodb": "^7.0.0",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.3",
    "solid-js": "^1.9.5",
    "tailwindcss": "^4.1.18",
    "vinxi": "^0.5.7",
    "zod": "^4.1.13"
  },
  "engines": {
    "node": ">=22"
  }
}
